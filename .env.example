# The Decision Engine - Environment Variables

# Required: Secret key for HMAC SHA-256 signing of the audit log chain.
# For demo purposes, the app will use a fallback key if not provided,
# but it is highly recommended to set this in production.
AUDIT_HMAC_SECRET="your_super_secret_hmac_key_here_at_least_32_chars"
